extends base
include mixins/homeVideo

block content 
    form(method="GET")
        input(type="text", placeholder="Search by title", name="keyword")
        input(type="submit", value="Search")

    div
        each video in videos 
            +video(video)